# T-18 – ofullständiga importfiler vägras

Status: klar på gren `codex/T-18`, byggd på `codex/T-17` enligt uppdraget.

- Filimporten kräver `events` som lista i v2 och `history` som lista i v0/v1 innan bekräftelserutan visas.
- Tom lista är giltig. Äldre filer får utelämna frivilliga måltids-/träningslistor; angivna listor måste ha rätt typ.
- Befintligt felmeddelande används. Ogiltig fil ändrar varken aktuell data eller en tidigare importbackup.
- Kontrollen ligger bara i `backup.js`. Dataversion, migrering av lagrad data och cache-version är oförändrade.
- T-17:s todo har tagits bort och åtta regressionstester har lagts till.

## Kontrollerat
- `node --check js/backup.js` och `node --check tests/backup.test.mjs`: godkänt.
- `node --test tests/*.test.mjs`: 59 godkända, 0 fel och 0 todo.
- Giltiga v0/v1-filer med fastor, måltider, träningspass och aktiv fasta migreras i testerna; export/import och Ångra import fungerar.
- Lokal webbläsare 390×844: äldre sparad v1-data uppgraderades och dess 16-timmarsfasta visades i Historik.
- Import av v2-fil utan `events` gav felruta; samma historikpost fanns kvar efteråt.
- Webbläsartestet använde en tillfällig kopia av appskalet som höll fil-inputen i DOM för testverktyget, samt konstgjorda data på separat lokal port. Appens riktiga moduler kördes. Provsidor/filer är borttagna.
- Full v0/v1-filimport är automatiskt testad men inte genomförd med filväljaren i webbläsaren. Fysisk telefon ej provad.
- Claude Code uppdaterar kön och höjer cache-versionen vid publicering.

## Anton kan se på datorn efter granskning
1. Profil → Importera data: en ofullständig fil ger fel direkt, utan fråga om att ersätta data.
2. Historik och Profil finns kvar efter det nekade försöket.
3. En giltig backup med tom lista kan fortfarande importeras efter bekräftelse.
4. Äldre kompletta backupfiler fungerar fortfarande.

## Granskning (Claude Code 2026-09-26)
- Kod granskad mot `migrate()`: kontrollen stämmer med vad v1/v2 läser; inga fynd.
- `main` (fasta-v37) ihopslagen i grenen: 90 tester gröna, 0 todo.
- 390×844, riktig filimport (fil-input i appen, alert/confirm fångade): v2 utan `events` och v1 utan `history` → "Filen kunde inte läsas…"; komplett v2, v2 med `events: []` och v1 med historik → bekräftelsen visas. Sparad data oförändrad efter alla försök.
