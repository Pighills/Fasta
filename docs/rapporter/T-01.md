# T-01 · Verifiera live efter 8a–8c

**Ändrat:** bara dokument (ingen appändring, ingen cache-höjning). Plus `.playwright-mcp/` i `.gitignore` (loggmapp från mätverktyget).

**QA** (`docs/qa/qa-efter-fixar.md`, 390×844 på fastatimer.se, fasta-v36): alla 15 fixade punkter (K1, K2, H1–H4, M4, M6, L3–L6, L13) fungerar live. Hälsopoäng 99/100 (preliminär; baslinjen 89 hade annan täckning). Inga konsolfel. All testdata låg i en tillfällig webbläsare, ingen riktig användardata.

**Prestanda** (`docs/benchmark/after-fixes.md`, samma skript som baslinjen): oförändrat betyg A. Mobil LCP 1,18 s (före 1,14 s). JavaScript 45,7 KB (+14 %, varning men väntat: ny kontroll- och skyddskod). Inga försämringar.

**Canary** (5 laddningar under 4 min): alla svar 200, laddtid 55–96 ms, inga konsolfel.

**Nya fel (inget fixat):**
- **Låg** – Timer → 🏋️ Träning: rutan visar "~25.0h extra fastaeffekt" för 2000 kcal fast taket (1,4×) gör att en ny fasta bara får några sekunder. Återskapa: starta fasta, öppna Träning, skriv 2000 kcal.
- Kodanteckning (inte synligt): Historik-detaljen räknar passets bonus med egen formel i `js/modals.js` i stället för `workoutBonusHours()`.

**Inte testat:** riktig telefon (offline, installerad app, notch), service worker-byte v35→v36, tillgänglighet.

**Avvikelse från arbetsbeskrivningen:** /benchmark kördes med projektets eget mätskript (`matning.js`) för att siffrorna ska gå att jämföra med baslinjen, och /canary som en kort egen slinga (gstacks canary kräver Aside, som inte finns på Windows).
