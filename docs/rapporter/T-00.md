# T-00 · Starta arbetsflödet

**Ändrat (bara dokument, ingen appändring, ingen cache-höjning):**
- Coworks `AGENTS.md` (avsnittet "Jobba vidare"), `docs/ARBETSFLODE.md` och `.gitignore` committade. `docs/arbete/` ligger utanför git (kontrollerat med `git check-ignore`).
- `.gstack/` ignoreras redan av sin egen `.gitignore`, så ingen rad behövdes.
- `docs/STATUS.md` bantad till 1,6 KB: Live nu, Pågår (hänvisar till kön), Verktyg, Kända problem. Alla "Klart"-avsnitt flyttade ordagrant till `docs/historik.md`, nyaste först. "Nästa steg" borttaget.
- `docs/fixplan.md`: rad överst om att kön styr arbetsordningen.
- Codex-kopia i `D:\FASTA-codex` med remote `lokal` → `D:/FASTA`, och remote `codex` i `D:\FASTA` (görs efter publicering).

**Krockar med övriga regler:** inga som stoppar något. Två saker att känna till:
- AGENTS.md säger att Anton säger "publicera"; ARBETSFLODE låter Claude Code publicera förgodkända uppgifter (bara `docs/`/`tests/`) utan att fråga. Det är ett medvetet undantag, men det står bara i ARBETSFLODE.
- AGENTS.md: "höj cache-versionen vid varje ändring som ska deployas"; ARBETSFLODE: höj först vid publicering, inte på grenen. Förenligt, eftersom höjningen ändå sker före deploy.

**Syns i appen:** nej.
